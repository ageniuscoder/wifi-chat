# Shared runtime configuration for the WiFi Chat Go server.
# Copy this to .env and adjust values per host.

# HTTP server port
PORT=8000

# Static upload directory used by the image upload API
UPLOAD_DIR=./uploads

#static msg persist directory
LOCAL_MSG_PERSIST_DIR=./data

# Upload limit in bytes for multipart image requests
MAX_UPLOAD_SIZE=10485760